# Native local development only. Copy deployment-specific templates from
# deploy/local/ or deploy/a100/ instead of placing production values here.
APP_NAME=Trip Helper
APP_ENV=development
APP_DOMAIN=localhost
PUBLIC_API_PREFIX=/api/v1
LOG_LEVEL=INFO

# PostgreSQL and Redis are local native services. The production A100 template
# binds both to loopback and keeps the API on WireGuard only.
DATABASE_URL=postgresql+psycopg://trip_helper:trip_helper_local_only@127.0.0.1:5432/trip_helper
REDIS_URL=redis://127.0.0.1:6379/0
CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/0
STORAGE_ROOT=./data/web-storage

SESSION_SECRET=local-development-session-secret-change-before-sharing
COOKIE_SECURE=false
TASKS_EAGER=false

OCR_PROVIDER=local_paddle
PADDLEOCR_API_URL=http://127.0.0.1:8118/v1
PADDLEOCR_MODEL=PaddleOCR-VL-1.5-0.9B
# Optional native MinerU service on an A100. Set the URL to enable it as a
# fallback after PaddleOCR, or use OCR_PROVIDER=mineru to make it primary.
MINERU_API_URL=
MINERU_ENABLED=true
MINERU_BACKEND=hybrid-engine
MINERU_PARSE_METHOD=auto
TESSERACT_FALLBACK_ENABLED=true
TESSERACT_COMMAND=tesseract
TESSERACT_LANGUAGES=chi_sim+eng
# Optional server-side fallback. Existing REMOTE_API_* variables remain
# supported for migration, but never expose these values to the browser.
REMOTE_OCR_ENABLED=false
REMOTE_OCR_API_URL=
REMOTE_OCR_API_KEY=
REMOTE_OCR_MODEL=Qwen3-VL-30B-A3B-Instruct

MAX_UPLOAD_BYTES=104857600
MAX_PDF_PAGES=100
MAX_IMAGE_PIXELS=40000000
