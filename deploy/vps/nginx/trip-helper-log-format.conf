# Install in /etc/nginx/conf.d/ before enabling trip-helper.conf.
# $uri deliberately excludes the query string, avoiding accidental logging of
# signed URLs or other query parameters.
log_format trip_helper '$request_id $request_method $uri $status $request_time';
